export class SelectOption {

    /**
     * @param {number} id the option id
     * @param {string} optionText the
     */
    constructor(id, optionText) {
        this.id = id;
        this.value = optionText;
    }

}