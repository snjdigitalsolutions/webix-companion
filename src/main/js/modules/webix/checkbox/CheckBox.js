import {Identifiable} from "../extendable/Identifiable.js";

export class CheckBox extends Identifiable {

    /**
     * @param {string} id the CheckBox ID
     */
    constructor(id) {
        super(id);
    }

}